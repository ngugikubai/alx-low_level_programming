#include <stdio.h>
#include <stdlib.h>
/**
 *main - entry point
 *Return: return 0 if successful
 */
int main(void)
{
char alphabet;
char 1 = '\n';
for (alphabet = 'a'; alphabet <= 'z'; alphabet++)
putchar(alphabet);
putchar(1);
return (0);
}
